version: '3'
services:
  web:
    restart: on-failure
    build:
      context: ../../app
      dockerfile: Dockerfile.dev
    volumes:
      - ../../app:/usr/src/app
    hostname: web
    ports:
      - '80:5000'
  redis:
    image: redis
    hostname: redis
    ports:
      - '6379:6379'
